name: web-xai
version: 1.0.0
description: "xAI Web Search — search the web via Grok's agentic web_search tool (Responses API). Requires xAI Grok OAuth (via `synapse auth`) or XAI_API_KEY (https://x.ai)."
author: Josh Research
kind: backend
provides_web_providers:
  - xai
